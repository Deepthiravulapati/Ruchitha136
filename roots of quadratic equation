#include <stdio.h>
#include<math.h>
int main() {
   float a,b,c;
   scanf("%d%d%d",&a,&b,&c);
   float d=b*b-4*a*c;
   if(d<0)
   {
       printf("roots are possible");
   }
   else{
   float r1=(-b+sqrt(d))/(2*a);
     float r2=(-b-sqrt(d))/(2*a);
     printf("Root1=%f\n",r1);
      printf("Root2=%f\n",r2);
   }
}
